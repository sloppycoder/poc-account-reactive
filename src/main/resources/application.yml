account:
  dummy: true

quarkus:
  datasource:
    db-kind: postgresql
    username: pocuser
    password: password
    jdbc:
      url: jdbc:${quarkus.datasource.reactive.url}
    reactive:
      url: postgresql://gf63.local:5432/poc
  log:
    console:
      format: "%d{HH:mm:ss} %-5p traceId=%X{traceId}, parentId=%X{parentId}, spanId=%X{spanId}, sampled=%X{sampled} [%c{2.}] (%t) %s%e%n"
  flyway:
    migrate-at-start: true
    baseline-on-migrate: false # setup to true to use a new schema
  jacoco:
    excludes: "**/model/*.class"
#     uncomment jaeger server-name will trigger sending trace info to jaeger endpoint
#     jaeger:
#       server-name: account-reactive